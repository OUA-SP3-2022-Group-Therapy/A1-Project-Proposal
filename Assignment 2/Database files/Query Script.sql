--Verify login request [Unfinished]
SELECT UserID, Email, Pass
FROM Account;

--Signup initial
INSERT INTO Account (UserID, Email, Pass, Date_Created)
VALUES (/*RANDINT*/, /*User input 1*/, /*User input 2*/, DATETIME('now');

--Signup secondary
INSERT INTO Account (User_Type, Gender)
VALUES (/*User_Type*/, /*Gender*/)

--Group details/Dashboard retrieve
SELECT *
FROM Family_Group
WHERE GroupID = /*Selected ID*/

--Family Group/Dashboard part 2 retrieve

--User Profile

--Pet Profile

--Add/Edit Schedule
